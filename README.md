## What makes this agent different from ChatGPT?

* Answer:

The agent maintains persistent lead history,
stores generated emails,
tracks company interactions,
and can build future outreach
using stored context.

## Why are we doing PostgreSQL?

POST /run-agent

↓
AI generates response

↓
Save to Database

↓
Response returned

↓
History preserved forever

This is what we call: Persistent Memory